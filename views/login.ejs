<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Autoryzacja Systemu</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            background-color: #0f172a;
            font-family: 'Poppins', sans-serif;
            margin: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }

        /* TŁO */
        .blobs { position: absolute; width: 100%; height: 100%; z-index: -1; }
        .blob { position: absolute; border-radius: 50%; filter: blur(80px); opacity: 0.4; }
        .b1 { top: -10%; left: -10%; width: 500px; height: 500px; background: #764ba2; animation: move 10s infinite alternate; }
        .b2 { bottom: -10%; right: -10%; width: 500px; height: 500px; background: #667eea; animation: move 8s infinite alternate-reverse; }

        @keyframes move { from { transform: translate(0,0); } to { transform: translate(40px, 40px); } }

        /* KARTA LOGOWANIA */
        .login-card {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 50px;
            border-radius: 24px;
            width: 350px;
            text-align: center;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        }

        .icon-lock {
            font-size: 3rem;
            margin-bottom: 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        h2 { color: #fff; margin: 0 0 10px; font-weight: 600; letter-spacing: 1px; }
        p { color: #94a3b8; margin-bottom: 30px; font-size: 0.9rem; }

        input {
            width: 100%;
            background: rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 15px;
            border-radius: 12px;
            color: #fff;
            margin-bottom: 15px;
            box-sizing: border-box; /* Ważne żeby padding nie rozpychał */
            text-align: center;
            font-size: 1rem;
            transition: 0.3s;
        }

        input:focus { outline: none; border-color: #667eea; background: rgba(0,0,0,0.5); }

        button {
            width: 100%;
            padding: 15px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            border-radius: 12px;
            color: white;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            transition: 0.3s;
            box-shadow: 0 10px 20px -5px rgba(118, 75, 162, 0.4);
        }

        button:hover { transform: translateY(-3px); box-shadow: 0 15px 30px -5px rgba(118, 75, 162, 0.6); }

        .error-msg {
            color: #ff4757;
            background: rgba(255, 71, 87, 0.1);
            padding: 10px;
            border-radius: 8px;
            font-size: 0.85rem;
            margin-bottom: 15px;
            display: none; /* Domyślnie ukryte */
        }
    </style>
</head>
<body>

    <div class="blobs">
        <div class="blob b1"></div>
        <div class="blob b2"></div>
    </div>

    <div class="login-card">
        <div class="icon-lock"><i class="fa-solid fa-user-shield"></i></div>
        <h2>SECURE ACCESS</h2>
        <p>Zaloguj się do systemu SzAP</p>

        <% if (locals.error) { %>
            <div class="error-msg" style="display: block;">
                <%= error %>
            </div>
        <% } %>

        <form action="/login" method="POST">
            <input type="text" name="login" placeholder="Login" required autocomplete="off">
            <input type="password" name="haslo" placeholder="Hasło dostępu" required>
            <button type="submit">Autoryzacja <i class="fa-solid fa-arrow-right"></i></button>
        </form>
    </div>

</body>
</html>