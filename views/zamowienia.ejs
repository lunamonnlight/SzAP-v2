<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>System Arsenał - Logistyka</title>
    
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" type="image/png" href="/logo-v2.png">
    <meta name="theme-color" content="#0f172a">

    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&family=Special+Elite&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        :root { --bg-color: #0f172a; --accent: #667eea; --glass: rgba(255,255,255,0.03); --border: rgba(255,255,255,0.1); --text: #fff; --warning: #f59e0b; }
        body { background: var(--bg-color); color: var(--text); font-family: 'Poppins', sans-serif; margin: 0; display: flex; min-height: 100vh; }
        
        .sidebar { width: 280px; background: rgba(15,23,42,0.8); backdrop-filter: blur(20px); border-right: 1px solid var(--border); padding: 40px 30px; display: flex; flex-direction: column; position: sticky; top: 0; height: 100vh; }
        .nav-item { padding: 14px 20px; margin-bottom: 8px; border-radius: 14px; color: #94a3b8; display: flex; align-items: center; gap: 15px; text-decoration: none; transition: 0.3s; }
        .nav-item:hover, .nav-item.active { background: rgba(255,255,255,0.05); color: #fff; }
        .logo { font-size: 1.5rem; font-weight: 800; margin-bottom: 60px; color: #fff; display:flex; align-items:center; gap:10px; }

        .main { flex: 1; padding: 50px; width: 100%; overflow-y: auto; }

        .tabs { display: flex; gap: 20px; margin-bottom: 30px; border-bottom: 1px solid var(--border); padding-bottom: 20px; }
        .tab-btn { background: none; border: none; color: #94a3b8; font-size: 1.1rem; cursor: pointer; padding: 10px 20px; border-radius: 10px; font-weight: bold; }
        .tab-btn.active { background: var(--accent); color: #fff; }

        .section { display: none; }
        .section.active { display: block; }

        /* TABELA */
        .table-container { background: var(--glass); border: 1px solid var(--border); border-radius: 20px; overflow: hidden; }
        table { width: 100%; border-collapse: collapse; }
        th, td { padding: 15px 20px; text-align: left; border-bottom: 1px solid var(--border); }
        th { background: rgba(0,0,0,0.2); color: #94a3b8; font-size: 0.85rem; text-transform: uppercase; }
        tr:hover { background: rgba(255,255,255,0.02); }

        .btn-primary { background: var(--accent); color: #fff; border: none; padding: 10px 20px; border-radius: 8px; cursor: pointer; font-weight: bold; }
        .input-dark { background: rgba(0,0,0,0.3); border: 1px solid var(--border); padding: 10px; border-radius: 8px; color: #fff; width: 100%; box-sizing: border-box; }

        /* DRUK */
        @media print {
            body { background: #fff !important; color: #000 !important; display: block; }
            .sidebar, .tabs, .main > h1, .btn-print-hide { display: none !important; }
            .order-print-view { display: block !important; position: absolute; top: 0; left: 0; width: 100%; height: 100%; z-index: 9999; background: #fff; padding: 40px; }
            .order-print-view h1 { color: #000; border-bottom: 2px solid #000; padding-bottom: 10px; }
            .order-print-view table { width: 100%; border-collapse: collapse; margin-top: 20px; }
            .order-print-view th { background: #eee; color: #000; border: 1px solid #000; }
            .order-print-view td { border: 1px solid #000; color: #000; }
        }
        .order-print-view { display: none; }
    </style>
</head>
<body>
    
    <div id="printArea" class="order-print-view"></div>

    <nav class="sidebar">
        <div class="logo"><i class="fa-solid fa-shield-halved"></i> SYSTEM ARSENAŁ</div>
        <a href="/" class="nav-item"><i class="fa-solid fa-layer-group"></i> Magazyn</a>
        <a href="/zamowienia" class="nav-item active"><i class="fa-solid fa-truck-fast"></i> Zamówienia</a>
        <a href="/historia" class="nav-item"><i class="fa-solid fa-list"></i> Logi</a>
        <div style="margin-top:auto">
            <form action="/logout" method="POST"><button style="background:none; border:none; color:#ff4757; cursor:pointer;">Wyloguj</button></form>
        </div>
    </nav>

    <main class="main">
        <h1><i class="fa-solid fa-clipboard-list"></i> Centrum Logistyki (PU4)</h1>
        
        <div class="tabs btn-print-hide">
            <button class="tab-btn active" onclick="switchTab('zamowienia')">Historia Zamówień</button>
            <button class="tab-btn" onclick="switchTab('nowe')">+ Nowe Zamówienie</button>
            <button class="tab-btn" onclick="switchTab('dostawcy')">Baza Dostawców</button>
        </div>

        <div id="zamowienia" class="section active">
            <% if(zamowienia.length === 0) { %>
                <p style="color:#aaa">Brak zamówień w systemie.</p>
            <% } else { %>
                <div class="table-container">
                    <table>
                        <thead><tr><th>Nr Zamówienia</th><th>Data</th><th>Dostawca</th><th>Kwota</th><th>Status</th><th>Akcja</th></tr></thead>
                        <tbody>
                            <% zamowienia.forEach(z => { %>
                                <tr>
                                    <td style="font-family:monospace; color:#accent"><%= z.id %></td>
                                    <td><%= z.data %></td>
                                    <td><%= z.dostawca ? z.dostawca.nazwa : 'Nieznany' %></td>
                                    <td><%= z.suma.toFixed(2) %> PLN</td>
                                    <td><span style="background:rgba(16,185,129,0.2); color:#10b981; padding:4px 10px; border-radius:10px; font-size:0.8rem"><%= z.status %></span></td>
                                    <td>
                                        <button onclick='printOrder(<%- JSON.stringify(z) %>)' class="btn-primary" style="padding:5px 10px; font-size:0.8rem;"><i class="fa-solid fa-print"></i> Drukuj</button>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            <% } %>
        </div>

        <div id="nowe" class="section">
            <form action="/zamowienia/nowe" method="POST">
                <div style="background:var(--glass); padding:20px; border-radius:20px; border:1px solid var(--border); margin-bottom:20px;">
                    <h3 style="margin-top:0">1. Wybierz Dostawcę</h3>
                    <select name="dostawcaId" class="input-dark" required>
                        <% dostawcy.forEach(d => { %>
                            <option value="<%= d.id %>"><%= d.nazwa %> (Email: <%= d.email %>)</option>
                        <% }) %>
                    </select>
                </div>

                <div class="table-container">
                    <h3 style="margin:20px">2. Skompletuj listę (Czerwone = Braki)</h3>
                    <table>
                        <thead><tr><th>Produkt</th><th>Stan / Minimum</th><th>Cena Jedn.</th><th>Zamawiana Ilość</th></tr></thead>
                        <tbody>
                            <% arsenal.forEach(item => { 
                                const isLow = item.min_ilosc && item.ilosc < item.min_ilosc;
                            %>
                                <tr style="<%= isLow ? 'background:rgba(239,68,68,0.1); border-left:4px solid #ef4444;' : '' %>">
                                    <td>
                                        <strong><%= item.nazwa %></strong><br>
                                        <span style="font-size:0.8rem; color:#aaa"><%= item.kod %></span>
                                    </td>
                                    <td>
                                        <%= item.ilosc %> / <%= item.min_ilosc || 0 %>
                                        <% if(isLow) { %> <i class="fa-solid fa-triangle-exclamation" style="color:#ef4444"></i> <% } %>
                                    </td>
                                    <td><%= item.cena ? item.cena + ' PLN' : '-' %></td>
                                    <td>
                                        <input type="number" name="item_<%= item.id %>" class="input-dark" style="width:100px" placeholder="0" min="0">
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
                
                <div style="margin-top:20px; text-align:right;">
                    <button type="submit" class="btn-primary" style="font-size:1.1rem; padding:15px 30px;">
                        <i class="fa-solid fa-paper-plane"></i> WYŚLIJ ZAMÓWIENIE
                    </button>
                </div>
            </form>
        </div>

        <div id="dostawcy" class="section">
            <div style="margin-bottom:20px; display:flex; gap:10px;">
                <form action="/dostawcy/dodaj" method="POST" style="display:flex; gap:10px; width:100%;">
                    <input type="text" name="nazwa" placeholder="Nazwa firmy" class="input-dark" required>
                    <input type="text" name="email" placeholder="Email" class="input-dark" required>
                    <input type="text" name="adres" placeholder="Adres" class="input-dark">
                    <button type="submit" class="btn-primary">+ Dodaj</button>
                </form>
            </div>
            
            <div class="table-container">
                <table>
                    <thead><tr><th>Firma</th><th>Email</th><th>Adres</th></tr></thead>
                    <tbody>
                        <% dostawcy.forEach(d => { %>
                            <tr>
                                <td><strong><%= d.nazwa %></strong></td>
                                <td><%= d.email %></td>
                                <td><%= d.adres %></td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </main>

    <script>
        function switchTab(id) {
            document.querySelectorAll('.section').forEach(s => s.classList.remove('active'));
            document.getElementById(id).classList.add('active');
            document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
            event.target.classList.add('active');
        }

        function printOrder(order) {
            const printArea = document.getElementById('printArea');
            let rows = '';
            order.pozycje.forEach((p, index) => {
                rows += `<tr>
                    <td>${index+1}</td>
                    <td>${p.nazwa} (${p.kod})</td>
                    <td>${p.ilosc}</td>
                    <td>${p.cena_jedn} PLN</td>
                    <td>${p.wartosc.toFixed(2)} PLN</td>
                </tr>`;
            });

            const html = `
                <div style="font-family: 'Times New Roman', serif;">
                    <div style="display:flex; justify-content:space-between; border-bottom:2px solid #000; padding-bottom:20px;">
                        <div>
                            <h1>ZAMÓWIENIE ${order.id}</h1>
                            <p>Data: ${order.data}</p>
                            <p>Status: <strong>${order.status}</strong></p>
                        </div>
                        <div style="text-align:right">
                            <h3>DOSTAWCA:</h3>
                            <p><strong>${order.dostawca.nazwa}</strong></p>
                            <p>${order.dostawca.adres}</p>
                            <p>${order.dostawca.email}</p>
                        </div>
                    </div>

                    <h3 style="margin-top:30px;">POZYCJE ZAMÓWIENIA</h3>
                    <table style="width:100%; border:1px solid #000; border-collapse:collapse;">
                        <thead>
                            <tr style="background:#eee;">
                                <th style="border:1px solid #000; padding:10px;">Lp</th>
                                <th style="border:1px solid #000; padding:10px;">Nazwa Towaru</th>
                                <th style="border:1px solid #000; padding:10px;">Ilość</th>
                                <th style="border:1px solid #000; padding:10px;">Cena Jedn.</th>
                                <th style="border:1px solid #000; padding:10px;">Wartość</th>
                            </tr>
                        </thead>
                        <tbody>${rows}</tbody>
                    </table>

                    <div style="text-align:right; margin-top:20px;">
                        <h2>SUMA: ${order.suma.toFixed(2)} PLN</h2>
                    </div>

                    <div style="margin-top:50px; display:flex; justify-content:space-between;">
                        <div>
                            <p>Wystawił:</p>
                            <p>.........................................<br>${order.wystawil}</p>
                        </div>
                        <div>
                            <p>Zatwierdził:</p>
                            <p>.........................................<br>Pieczątka i podpis</p>
                        </div>
                    </div>
                </div>
            `;
            
            printArea.innerHTML = html;
            window.print();
        }
    </script>
</body>
</html>