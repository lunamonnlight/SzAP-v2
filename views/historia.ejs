<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <title>Rejestr Operacji Systemowych</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&family=Fira+Code:wght@400&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        :root {
            --bg-color: #0f172a;
            --glass-bg: rgba(255, 255, 255, 0.03);
            --glass-border: 1px solid rgba(255, 255, 255, 0.1);
            --text-primary: #ffffff;
            --terminal-green: #10b981;
            --terminal-red: #ff4757;
            --terminal-blue: #3b82f6;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-primary);
            font-family: 'Poppins', sans-serif;
            margin: 0; display: flex; min-height: 100vh;
        }

        .sidebar {
            width: 280px; background: rgba(15, 23, 42, 0.4); backdrop-filter: blur(20px);
            border-right: var(--glass-border); padding: 40px 30px; display: flex; flex-direction: column;
        }
        .logo { font-size: 1.8rem; font-weight: 800; margin-bottom: 60px; color: #fff; }
        
        /* Linki w menu */
        a { text-decoration: none; }
        .nav-item { padding: 14px 20px; margin-bottom: 8px; border-radius: 14px; color: #94a3b8; display: flex; align-items: center; gap: 15px; font-weight: 500; transition: 0.3s; }
        .nav-item:hover { background: rgba(255,255,255,0.05); color: #fff; }
        .nav-item.active { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: #fff; }

        .main { flex: 1; padding: 50px; overflow-y: auto; }

        /* TABELA LOGÓW */
        .log-container {
            background: #000;
            border: 1px solid #333;
            border-radius: 10px;
            padding: 20px;
            font-family: 'Fira Code', monospace; /* Czcionka koderska */
            box-shadow: 0 0 30px rgba(0,0,0,0.5);
        }

        .log-header {
            display: grid;
            grid-template-columns: 200px 150px 1fr;
            padding-bottom: 15px;
            border-bottom: 1px solid #333;
            color: #666;
            text-transform: uppercase;
            font-size: 0.8rem;
            margin-bottom: 10px;
        }

        .log-row {
            display: grid;
            grid-template-columns: 200px 150px 1fr;
            padding: 12px 0;
            border-bottom: 1px dashed #222;
            align-items: center;
            transition: 0.2s;
        }

        .log-row:hover { background: rgba(255,255,255,0.05); }

        .log-date { color: #888; font-size: 0.9rem; }
        
        .badge {
            display: inline-block; padding: 4px 10px; border-radius: 4px; 
            font-size: 0.75rem; font-weight: bold; text-align: center; width: fit-content;
        }
        
        .type-add { color: var(--terminal-green); border: 1px solid var(--terminal-green); background: rgba(16, 185, 129, 0.1); }
        .type-del { color: var(--terminal-red); border: 1px solid var(--terminal-red); background: rgba(255, 71, 87, 0.1); }
        .type-mod { color: var(--terminal-blue); border: 1px solid var(--terminal-blue); background: rgba(59, 130, 246, 0.1); }

        .log-desc { color: #ddd; }

    </style>
</head>
<body>

    <nav class="sidebar">
        <div class="logo"><i class="fa-solid fa-cube"></i> ARSENAŁ</div>
        
        <a href="/" class="nav-item">
            <i class="fa-solid fa-layer-group"></i> Stan Magazynu
        </a>
        
        <a href="/historia" class="nav-item active">
            <i class="fa-solid fa-clipboard-list"></i> Rejestr Operacji
        </a>

        <div style="margin-top: auto;">
            <form action="/logout" method="POST" style="margin:0">
                <button style="background:none; border:none; width:100%; padding:0;">
                    <div class="nav-item" style="color: #ff4757;"><i class="fa-solid fa-right-from-bracket"></i> Wyloguj</div>
                </button>
            </form>
        </div>
    </nav>

    <main class="main">
        <header style="margin-bottom: 40px;">
            <h1 style="margin:0">Rejestr Zdarzeń</h1>
            <p style="color:#94a3b8; margin:5px 0 0">Pełna historia operacji na magazynie [READ-ONLY]</p>
        </header>

        <div class="log-container">
            <div class="log-header">
                <div>Sygnatura Czasowa</div>
                <div>Typ Operacji</div>
                <div>Szczegóły Zdarzenia</div>
            </div>

            <% if(logi.length === 0) { %>
                <div style="padding: 20px; text-align: center; color: #444;">-- BRAK WPISÓW W REJESTRZE --</div>
            <% } %>

            <% logi.forEach(wpis => { %>
                <div class="log-row">
                    <div class="log-date"><%= wpis.data %></div>
                    
                    <div>
                        <% if (wpis.typ === 'DOSTAWA') { %>
                            <span class="badge type-add">DOSTAWA [+]</span>
                        <% } else if (wpis.typ === 'LIKWIDACJA') { %>
                            <span class="badge type-del">USUNIĘCIE [!]</span>
                        <% } else { %>
                            <span class="badge type-mod">MODYFIKACJA</span>
                        <% } %>
                    </div>
                    
                    <div class="log-desc"><%= wpis.opis %></div>
                </div>
            <% }) %>
        </div>
    </main>

</body>
</html>